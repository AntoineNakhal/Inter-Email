# Shared

This folder is for cross-application contracts.

Because the frontend and backend use different languages, the best shared contract is the API schema, not hand-copied model files.

Recommended use:

- generated or committed OpenAPI artifacts
- frontend type generation inputs
- example payloads for integration tests
